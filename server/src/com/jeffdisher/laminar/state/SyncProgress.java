package com.jeffdisher.laminar.state;

import com.jeffdisher.laminar.types.ClusterConfig;


public class SyncProgress {
	public final ClusterConfig config;

	public SyncProgress(ClusterConfig config) {
		this.config = config;
	}
}
